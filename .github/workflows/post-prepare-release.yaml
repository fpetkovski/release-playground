name: post-prepare-please
on:
  pull_request:
jobs:
  post-release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: make generate
      - echo "$GITHUB_REF"
      
